#!/bin/sh

# Load .env.local if it exists (for untracked secrets)
# - env-file flags don't work in NODE_OPTIONS so invoke node directly
# - point node at vite's JS entry: pnpm's .bin/vite is a shell shim node can't parse
exec node --env-file-if-exists=.env.local node_modules/vite/bin/vite.js
