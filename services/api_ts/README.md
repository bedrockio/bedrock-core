# API_ts Typescript

```bash
cd services/api;
rm -rf node_modules
yarn global add typescript
yarn install
yarn build # build js from ts files
yarn test # runs jest on js files in built folder
yarn start # runs nodemon on js in built folder
yarn dev # runs ts-node on index.ts
```